# Models manifest — repo IDs grouped by category.
# Run `ai-models pull` to download all.


llm:
  - arthurcollet/Qwen3.6-35B-A3B-mlx-mxfp8
  - arthurcollet/Qwen3.6-27B-mlx-mxfp8

embedding:
  # Tiny memory footprint, Fast inference
  - mlx-community/all-MiniLM-L6-v2-4bit
  # good middle ground (Gemma backbone, trained for embeddings, multilingual)
  # - mlx-community/embeddinggemma-300m-6bit
  # top-tier retrieval quality at similar memory cost, only English
  # NOT EXIST - mlx-community/bge-large-en-v1.5-4bit
  # BGE-M3 is battle-tested for search/RAG, specifically trained for retrieval tasks (dense + sparse + multi-vector)
  - mlx-community/bge-m3-mlx-4bit
