# Models manifest — repo IDs grouped by category.
# Run `ai-models pull` to download all, or `ai-models pull <repo>` for one.
# Storage: HF cache layout under ${AI_HOME}/models/

llm:
  - arthurcollet/Qwen3.6-35B-A3B-mlx-mxfp8
  - arthurcollet/Qwen3.6-27B-mlx-mxfp8

embedding:
  - mlx-community/bge-m3-4bit
