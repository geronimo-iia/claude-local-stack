#!/bin/zsh
set -euo pipefail

if claude plugin list 2>/dev/null | grep -q "context-mode@context-mode"; then
  claude plugin uninstall --keep-data context-mode@context-mode
  echo "  ✓ context-mode removed"
else
  echo "  ✓ context-mode (not installed)"
fi
