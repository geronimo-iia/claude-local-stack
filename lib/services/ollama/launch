#!/bin/zsh
set -euo pipefail

source "$(dirname "$0")/../../../config/ai-stack.env"
source "${AI_HOME}/lib/utils/load-service-env" "ollama"

exec "${AI_HOME}/lib/utils/supervised-launch" ollama serve
