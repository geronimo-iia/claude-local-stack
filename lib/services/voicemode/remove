#!/bin/zsh
set -euo pipefail

SERVICE_DIR="${AI_HOME}/lib/components/voicemode"

if [[ -d "${SERVICE_DIR}/.venv" ]]; then
  rm -rf "${SERVICE_DIR}/.venv"
  echo "  ✓ voicemode removed"
else
  echo "  ✓ voicemode (not installed)"
fi
